import { MobileShell } from '@/components/MobileShell';

export default function KonfirmasiRuteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <MobileShell>{children}</MobileShell>;
}
